# SPDX-License-Identifier: MIT
# Copyright (C) 2026 VIKINGYFY

#CI项目：只编译友善 NanoPi R5C（OpenWrt 官方源码）
name: R5C

#CI计划
on:
  #自动编译：当Auto-Clean运行完成后
  workflow_run:
    workflows: ["Auto-Clean"]
    types:
      - completed
  #手动编译
  workflow_dispatch:
    inputs:
      PACKAGE:
        description: '手动调整插件包，多个请用\n符号隔开。'
        required: false
        type: string
      TEST:
        description: '仅输出配置文件，不编译固件。'
        default: 'false'
        required: false
        type: boolean

#CI权限
permissions: write-all

#CI任务
jobs:
  r5c:
    name: R5C
    #调用公用编译核心WRT-CORE.yml
    uses: ./.github/workflows/WRT-CORE.yml
    with:
      #编译配置
      WRT_CONFIG: R5C
      #默认主题（Argon，第三方：jerrykuku/luci-theme-argon）
      WRT_THEME: argon
      #默认主机名
      WRT_NAME: 狗鹏
      #默认WIFI名称
      WRT_SSID: 狗鹏
      #默认WIFI密码
      WRT_WORD: 12345678
      #默认地址
      WRT_IP: 192.168.1.1
      #默认 root 密码：Scripts/Settings.sh 会用它生成 sha512 哈希写进固件的 /etc/shadow
      #填「无」= 空密码（此时 dropbear / vsftpd 会拒绝登录，SSH/FTP 服务开着也进不去）；
      #填一个密码，刷完就能直接 SSH / FTP 登录（但公开仓库里等于把密码公开，自己权衡）
      WRT_PW: 无
      #源码链接（OpenWrt 官方）
      WRT_REPO: https://github.com/openwrt/openwrt.git
      #源码分支（25.12 稳定分支）
      WRT_BRANCH: openwrt-25.12
      #源码名称
      WRT_SOURCE: openwrt/openwrt
      #插件调整
      WRT_PACKAGE: ${{inputs.PACKAGE}}
      #仅输出配置文件
      WRT_TEST: ${{inputs.TEST}}
