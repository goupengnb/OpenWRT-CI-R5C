# 友善 NanoPi R5C（Rockchip RK3568B2）/ OpenWrt 官方 openwrt-25.12
# 只放设备 / 内核 / 网卡配置，业务插件在 GENERAL.txt

#---------- 目标平台与设备（rockchip / armv8，只编译 R5C 一个设备）----------
CONFIG_TARGET_rockchip=y
CONFIG_TARGET_rockchip_armv8=y
CONFIG_TARGET_MULTI_PROFILE=y
CONFIG_TARGET_PER_DEVICE_ROOTFS=y
CONFIG_TARGET_DEVICE_rockchip_armv8_DEVICE_friendlyarm_nanopi-r5c=y

#---------- 镜像格式 ----------
CONFIG_TARGET_ROOTFS_SQUASHFS=y
CONFIG_TARGET_ROOTFS_EXT4FS=y
CONFIG_TARGET_IMAGES_GZIP=y
CONFIG_TARGET_ROOTFS_INITRAMFS=n

#---------- 分区大小 ----------
#官方默认 boot 16M / rootfs 104M 装不下 Docker + AdGuard Home；前 32M 给 u-boot，
#剩下约 30G 留给 eMMC 自行分区（见 Scripts/Settings.sh 的 docker-storage）
CONFIG_TARGET_KERNEL_PARTSIZE=64
CONFIG_TARGET_ROOTFS_PARTSIZE=2048

#---------- 双 2.5G 网口（RTL8125B）----------
CONFIG_PACKAGE_kmod-r8169=y

#---------- WiFi（M.2 E-Key / PCIe 插槽）----------
CONFIG_PACKAGE_kmod-mt7921e=y
CONFIG_PACKAGE_kmod-mt7921-firmware=y
CONFIG_PACKAGE_wpad-openssl=y
# CONFIG_PACKAGE_wpad-basic-mbedtls is not set

#官方设备定义给 DEVICE_PACKAGES 写死了 wpad-basic-mbedtls（无条件叠进镜像），会和 wpad-openssl
#冲突；用 DEVICE_EXTRA_PACKAGES 的减号语法移除，RTL8822CE 的驱动/固件同理。
CONFIG_TARGET_DEVICE_PACKAGES_rockchip_armv8_DEVICE_friendlyarm_nanopi-r5c="-wpad-basic-mbedtls -kmod-rtw88-8822ce -rtl8822ce-firmware"

#---------- 内核 / 网络加速 ----------
#BBR（kmod-tcp-bbr 自带 sysctl）
CONFIG_PACKAGE_kmod-tcp-bbr=y
#BBR 要配 fq；sch_fq 不在 Config-kernel.in 里，由 Scripts/Settings.sh 给内核 config 补
#容器 / 代理需要的网络组件
CONFIG_PACKAGE_kmod-veth=y
CONFIG_PACKAGE_kmod-br-netfilter=y
CONFIG_PACKAGE_kmod-tun=y
CONFIG_PACKAGE_kmod-nf-nat6=y
