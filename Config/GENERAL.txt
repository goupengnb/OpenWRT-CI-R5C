# 通用配置（业务插件）
# 官方 feeds 里有的用官方的；没有的用仍在维护的第三方（shadcn / HomeProxy / ddns-go）

#---------- 编译全局参数 ----------
CONFIG_CCACHE=y
CONFIG_DEVEL=y

#---------- LuCI 界面（官方 luci feed）----------
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-ssl=y
CONFIG_LUCI_LANG_zh_Hans=y
#shadcn：第三方主题，只依赖 luci-base；自带 uci-defaults 会把它设成默认
CONFIG_PACKAGE_luci-theme-shadcn=y
CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-app-package-manager=y
#带宽监控（菜单被挪到 网络 → 带宽监控）
CONFIG_PACKAGE_luci-app-nlbwmon=y
CONFIG_PACKAGE_nlbwmon=y
#已移除：luci-app-ttyd / filemanager / commands / irqbalance（ttyd 二进制保留给 dockerman）

#---------- 系统工具 ----------
CONFIG_PACKAGE_bash=y
CONFIG_PACKAGE_nano=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_wget-ssl=y
CONFIG_PACKAGE_rsync=y
CONFIG_PACKAGE_ca-certificates=y
CONFIG_PACKAGE_openssl-util=y
CONFIG_PACKAGE_ip-full=y
CONFIG_PACKAGE_ethtool=y
CONFIG_PACKAGE_pciutils=y
CONFIG_PACKAGE_usbutils=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_tcpdump=y
CONFIG_PACKAGE_openssh-keygen=y
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_zoneinfo-core=y
CONFIG_PACKAGE_zoneinfo-asia=y

#---------- 存储 / USB ----------
#ext4 / f2fs 与 USB3 / eMMC / PCIe 都内建在 rockchip 内核里，不需要额外 kmod
CONFIG_PACKAGE_block-mount=y
CONFIG_PACKAGE_blkid=y
CONFIG_PACKAGE_lsblk=y
CONFIG_PACKAGE_fdisk=y
CONFIG_PACKAGE_sfdisk=y
CONFIG_PACKAGE_parted=y
CONFIG_PACKAGE_e2fsprogs=y
CONFIG_PACKAGE_dosfstools=y
CONFIG_PACKAGE_f2fs-tools=y
CONFIG_PACKAGE_btrfs-progs=y
#清旧签名 / 建 XFS / 管 swap
CONFIG_PACKAGE_wipefs=y
CONFIG_PACKAGE_xfs-mkfs=y
CONFIG_PACKAGE_swap-utils=y
CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_kmod-fs-exfat=y
CONFIG_PACKAGE_exfat-mkfs=y
CONFIG_PACKAGE_exfat-fsck=y
CONFIG_PACKAGE_kmod-fs-ntfs3=y
CONFIG_PACKAGE_kmod-fs-btrfs=y
#挂载 NAS 上的 SMB 共享
CONFIG_PACKAGE_kmod-fs-cifs=y
CONFIG_PACKAGE_cifsmount=y
CONFIG_PACKAGE_kmod-nls-utf8=y
CONFIG_PACKAGE_kmod-nls-cp437=y
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_kmod-usb-storage-uas=y
#USB 2.5G 网卡（RTL8152/8153/8156）备用
CONFIG_PACKAGE_kmod-usb-net-rtl8152=y
CONFIG_PACKAGE_smartmontools=y
#已移除 hdparm（磁盘休眠）、luci-app-diskman（第三方磁盘面板）

#---------- DNS（只留 AdGuard Home）----------
#dnsmasq-full：系统基础解析 + DHCP，AdGuard Home 和 HomeProxy 都挂在它上面
CONFIG_PACKAGE_dnsmasq-full=y
# CONFIG_PACKAGE_dnsmasq is not set
CONFIG_PACKAGE_adguardhome=y
CONFIG_PACKAGE_luci-app-adguardhome=y
#已移除 SmartDNS / https-dns-proxy（会抢 53 端口）

#---------- 网络服务（只留 ddns-go）----------
#第三方 ddns-go（支持 Cloudflare / 阿里 / 腾讯等）；官方 luci-app-ddns 已移除
CONFIG_PACKAGE_ddns-go=y
CONFIG_PACKAGE_luci-app-ddns-go=y
#已移除 UPnP / WOL / SQM

#---------- 文件共享（SMB / FTP）----------
#只装 samba4-server 本体，不装 luci-app-samba4 面板（LuCI 里没有「网络共享」菜单）；
#共享由 uci-defaults 建好：files → /opt/files，访客可读写。SMB 不能用 root（模板写死）。
CONFIG_PACKAGE_samba4-server=y
#vsftpd：默认值（local_root=/opt/files 等）由 Scripts/Handles.sh 写进包
CONFIG_PACKAGE_vsftpd=y

#---------- 代理 / 分流 ----------
#HomeProxy：第三方（ImmortalWrt 官方团队），sing-box 内核来自官方 feed，不需要 luci-compat
CONFIG_PACKAGE_luci-app-homeproxy=y
CONFIG_PACKAGE_sing-box=y
#HomeProxy 要用的、上游 Makefile 没声明的：ucode-mod-math + coreutils-nohup/timeout
CONFIG_PACKAGE_ucode-mod-math=y
CONFIG_PACKAGE_coreutils-nohup=y
CONFIG_PACKAGE_coreutils-timeout=y
#已移除 Tailscale / PBR / OpenVPN / WireGuard（含 kmod-wireguard），代理只留 HomeProxy
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_kmod-nft-socket=y
CONFIG_PACKAGE_kmod-nft-fib=y

#---------- Docker ----------
CONFIG_PACKAGE_luci-app-dockerman=y
CONFIG_PACKAGE_dockerd=y
CONFIG_PACKAGE_docker=y
CONFIG_PACKAGE_docker-compose=y
